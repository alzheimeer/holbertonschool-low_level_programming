#include "lists.h"

/**
 * listint_len - return number of elements
 * @h: header
 * Return: number of elements
 */

size_t listint_len(const listint_t *h)
{
	listint_t *aux;

	aux = h;
	int nn = 0;

	while (aux)
	{
		aux = aux->next;
		nn++;
	}
	return (nn);
}
